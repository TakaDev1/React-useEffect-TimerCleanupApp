## **問題4：タイマーとクリーンアップ**

1秒ごとにカウントが増えるタイマーを作成し、コンポーネントがアンマウントされるとタイマーを解除するコンポーネントを作成せよ。

---

### **条件**

(1) `count` を `useState` で管理すること
(2) `setInterval` を `useEffect` 内で設定し、アンマウント時に `clearInterval` すること
(3) TailwindCSSで背景を紫、文字を白にすること
